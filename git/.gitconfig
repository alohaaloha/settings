# global config
[user]
         name = alohaaloha
         email = <TEMP_USER>
[credential]
         helper = cache --timeout 3600

# enforce ssh
[url "git@github.com:"]
        insteadOf = https://github.com/
         
